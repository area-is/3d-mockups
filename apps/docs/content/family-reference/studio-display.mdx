The Studio Display rendered in WebGL, with a live React tree on its surfaces. Change
anything in the inspector and the `demo.tsx` panel rewrites itself to exactly
what is being passed.

<MockupExplorer component="StudioDisplayMockup" />

## Full API

Every prop, region and colorway this family accepts is documented on the
[`StudioDisplayMockup` reference](/docs/api/studio-display).
