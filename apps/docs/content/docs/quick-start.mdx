---
title: Quick start
description: Render your first 3D phone with a single component.
---

Drop a `GalaxyMockup` anywhere in a client component and give it a height. Your
content becomes the phone's screen.

```tsx title="hero.tsx"
'use client'

import { GalaxyMockup } from 'area-mockups'

export function Hero() {
  return (
    <div style={{ height: 560 }}>
      <GalaxyMockup autoRotate float>
        <YourApp /> {/* any React node */}
      </GalaxyMockup>
    </div>
  )
}
```

The mockup fills its parent, so size the wrapper, not the component.

## What you get out of the box

- **Drag to orbit** from anywhere: the body, the background, or the screen
  itself.
- **A live screen**: the content is real DOM projected onto the glass, so state,
  effects, and media playback all keep running, and text stays vector crisp at
  every angle. Screens are display-only by default so hardware can cover them
  pixel-perfectly; pass `interactive` when the content has to be clicked, and
  read [what that trades away](/docs/screen-content#interaction) first.
- **Studio lighting and soft shadows**, generated on the GPU with no HDR file
  downloads.
- **A transparent canvas** by default, so your page background shows through.

## Next.js note

WebGL only exists in the browser. In the App Router, load the mockup client
side only. See [Next.js and SSR](/docs/nextjs) for the pattern.
