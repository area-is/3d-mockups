---
title: Galaxy Z Flip 7
description: Live prop explorer for the Galaxy Z Flip 7 mockup - drive every prop and copy the code it generates.
---

The Galaxy Z Flip 7 rendered in WebGL, with a live React tree on its surfaces. Change
anything in the inspector and the `demo.tsx` panel rewrites itself to exactly
what is being passed.

<MockupExplorer component="FlipMockup" />

## Full API

Every prop, region and colorway this family accepts is documented on the
[`FlipMockup` reference](/docs/api/flip).
