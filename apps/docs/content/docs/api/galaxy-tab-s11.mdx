---
title: Galaxy Tab S11
description: Live prop explorer for the Galaxy Tab S11 mockup - drive every prop and copy the code it generates.
---

The Galaxy Tab S11 rendered in WebGL, with a live React tree on its surfaces. Change
anything in the inspector and the `demo.tsx` panel rewrites itself to exactly
what is being passed.

<MockupExplorer component="GalaxyTabMockup" variant="tabs11" />

## Full API

Every prop, region and colorway this family accepts is documented on the
[`GalaxyTabMockup` reference](/docs/api/galaxy-tab); this page pins `variant="tabs11"`.
