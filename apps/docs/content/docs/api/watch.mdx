---
title: Watch (Apple Watch + Galaxy Watch)
description: Apple Watch Series 11 or Galaxy Watch 8, wearing a two-strap wristband.
---

`<Watch>` and `<WatchMockup>` render a smartwatch wearing a real wristband on
an invisible wrist directly behind the case — two domed, tapering straps
sliding into the band slots in the case edges and meeting at a closure on the
underside of the wrist, matching how worn-band product photos pose it. Two
variants at true relative sizes:

- **`series11`** (default): Apple Watch Series 11, 46 mm — squircle case,
  gear-toothed knurled Digital Crown, flush side button in its machined
  recess, drilled mic hole, machined speaker slot on the left edge, a Sport
  Band closing with the pin-and-tuck (the twelve-o'clock strap laps over the
  other, its pin stud on show), and a live 208×248 rounded-rect face.
- **`watch8`**: Samsung Galaxy Watch 8, 44 mm — cushion case with the fully
  **round** 1.47" display raised on its dial puck (live 240×240 circular
  face), two raised chamfered keys straddling the mic hole, twin machined
  speaker slots on the left edge, and a Dynamic-Lug-style band: nearly
  case-wide at the connector, tapering around the wrist to a stainless pin
  buckle, a keeper and punched adjustment holes.

<ObjectDemo demo="watch-band" checker />

```tsx
<WatchMockup float color="#1c1d21" bandColor="#33415c">
  <YourWatchFace />
</WatchMockup>
```

## Props

Beyond the [shared screen props](/docs/screen-content):

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `variant` | `'series11' \| 'watch8'` | `'series11'` | Which watch to render. |
| `color` | `string` | `'#1c1d21'` | Case colorway. |
| `bandColor` | `string` | `'#2a2c31'` | Strap (fluoroelastomer). |
| `resolution` | `number` | per variant | Virtual display width: 208 (Apple point grid) or 240 (Galaxy dp grid). |

There is no `orientation` prop.

## Mockup-able areas

Every live surface filled with chroma green, labeled with the prop that
feeds it — the fastest way to see exactly where your content lands and
what stays 3D hardware.

<ObjectDemo demo="chroma-watch" />

## More examples

### Galaxy Watch 8, round face

The display is a circle: give your face a `border-radius: 50%` root (or just
design edge-to-edge — the crystal masks it round).

<ObjectDemo demo="watch-galaxy" />

```tsx
<WatchMockup variant="watch8" color="#33363c" bandColor="#23252a" float>
  <YourRoundFace />
</WatchMockup>
```
