---
title: Galaxy S26
description: Live prop explorer for the Galaxy S26 mockup - drive every prop and copy the code it generates.
---

The Galaxy S26 rendered in WebGL, with a live React tree on its surfaces. Change
anything in the inspector and the `demo.tsx` panel rewrites itself to exactly
what is being passed.

<MockupExplorer component="GalaxyMockup" variant="s26" />

## Full API

Every prop, region and colorway this family accepts is documented on the
[`GalaxyMockup` reference](/docs/api/galaxy); this page pins `variant="s26"`.
