---
title: Galaxy Z Fold 7
description: Live prop explorer for the Galaxy Z Fold 7 mockup - drive every prop and copy the code it generates.
---

The Galaxy Z Fold 7 rendered in WebGL, with a live React tree on its surfaces. Change
anything in the inspector and the `demo.tsx` panel rewrites itself to exactly
what is being passed.

<MockupExplorer component="FoldMockup" />

## Full API

Every prop, region and colorway this family accepts is documented on the
[`FoldMockup` reference](/docs/api/fold).
