---
title: iPhone 17 Air
description: Live prop explorer for the iPhone 17 Air mockup - drive every prop and copy the code it generates.
---

The iPhone 17 Air rendered in WebGL, with a live React tree on its surfaces. Change
anything in the inspector and the `demo.tsx` panel rewrites itself to exactly
what is being passed.

<MockupExplorer component="IPhoneMockup" variant="air" />

## Full API

Every prop, region and colorway this family accepts is documented on the
[`IPhoneMockup` reference](/docs/api/iphone); this page pins `variant="air"`.
